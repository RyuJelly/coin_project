html
    head 
    body 
        h3 코인 리스트
        form(action="/ppost", method="post")
            select(name="coin")
                -for(let i=0; i< coin_list.length; i++)
                    <option value=`${coin_list[i]}` name="text" class="sel"></option>
            input(type="date" id="startDate" value=weekAgo min="2010-01-01" name="startDate")
            input(type="date" id="endDate" value=today min="2010-01-01" name="endDate")
            input(type="submit" value="검색")
        ul 
            -for(let i=0; i< coin_list.length; i++)
                a(href=`/${coin_list[i]}`)
                    li= coin_list[i]

        script 
            -for(let i=0; i< coin_list.length; i++)
                //- -let a = document.getElementsByClassName('sel')